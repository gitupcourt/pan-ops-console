fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3

# Auth / crypto
argon2-cffi==23.1.0
PyJWT==2.9.0
cryptography>=43.0.1
python-multipart==0.0.12

# Background jobs
celery[redis]==5.4.0
redis==5.1.1

# PAN-OS
pan-os-python>=1.12.1
panos-upgrade-assurance==2.1.1

# Utilities
httpx==0.27.2
tenacity==9.0.0
python-dotenv==1.2.2

# Dev: lets `watchmedo` restart Celery when backend/app/** changes.
# Without this, code edits don't reach long-lived worker/beat processes.
watchdog==5.0.3
