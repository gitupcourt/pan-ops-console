[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short
filterwarnings =
    # SQLAlchemy + Python 3.11 noise we don't control
    ignore::DeprecationWarning
