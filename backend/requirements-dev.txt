# Dev-only deps. NOT installed into the production image. CI installs
# these alongside requirements.txt before running tests.
pytest==9.1.1
# pytest-asyncio removed: the suite has no async tests / fixtures / asyncio
# markers (Starlette's TestClient is sync), so it was an unused dependency —
# and it was the only thing pinning pytest <9. Re-add it if async tests land.
# FastAPI's TestClient needs httpx (the prod stack already has it but
# pinning explicitly here so dev installs are reproducible too).
httpx==0.28.1
